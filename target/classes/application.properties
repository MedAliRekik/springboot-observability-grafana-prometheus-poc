
# Port HTTP de l'application
server.port=8080

# Nom de l'application (utilis� comme tag dans les m�triques)
spring.application.name=observability-poc

# Actuator : on expose seulement les endpoints utiles
management.endpoints.web.exposure.include=health,info,prometheus

# Tag par d�faut sur toutes les m�triques
management.metrics.tags.application=${spring.application.name}
